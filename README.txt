The project uses cmake to generate the make files which inturn is used to
build the system. To compile:
    cd build (create directory if not present)
    cmake ..
    make

The compiled programs will be placed in 'bin'.

headers contain header files.
src contains implementation for headers.
tets contains code that is used to generate individual programs.
